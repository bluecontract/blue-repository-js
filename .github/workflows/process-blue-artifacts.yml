name: Process Blue Artifacts

on:
  repository_dispatch:
    types: [blue-preprocessor-completed]
  workflow_dispatch:
    inputs:
      run_id:
        description: 'Run ID of the blue-preprocessor workflow'
        required: true
        type: string

env:
  BASE_BRANCH: feature/gha

jobs:
  ProcessArtifacts:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout main repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.WORKFLOW_PAT }}
          fetch-depth: 0 # Fetch all history
          ref: ${{ env.BASE_BRANCH }} # Checkout the base branch directly

      - name: Sparse checkout repository
        uses: actions/checkout@v4
        with:
          repository: bluecontract/blue-repository
          token: ${{ secrets.WORKFLOW_PAT }}
          path: data/blue-repository
          sparse-checkout: |
            /*
            !/.github
            !/.gitignore
            !/.git
            !/.vscode
            !/.idea
          sparse-checkout-cone-mode: false

      - name: Download artifact from first workflow
        uses: dawidd6/action-download-artifact@v2
        with:
          github_token: ${{ secrets.WORKFLOW_PAT }}
          workflow: blue-preprocessor.yml
          run_id: ${{ github.event.client_payload.run_id || inputs.run_id }}
          name: blue-preprocessed
          repo: bluecontract/blue-repository
          path: data/blue-preprocessed

      - name: Display content of blue-ids.yaml
        run: |
          echo "Content of blue-ids.yaml:"
          cat data/blue-preprocessed/blue-ids.yaml

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run script
        run: npm run artifact-scaffolder:generate

      # - name: Create new branch
      #   run: |
      #     current_date=$(date +'%Y%m%d-%H%M%S')
      #     branch_name="feature/blue-artifacts-$current_date"
      #     git config --global user.name 'github-actions[bot]'
      #     git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'

      #     echo "Fetching and checking out feature/gha branch..."
      #     git fetch origin ${{ env.BASE_BRANCH }}
      #     git checkout ${{ env.BASE_BRANCH }}

      #     echo "Creating new branch..."
      #     git checkout -b $branch_name

      #     echo "BRANCH_NAME=$branch_name" >> $GITHUB_ENV

      # - name: Commit changes
      #   run: |
      #     echo "Current git status:"
      #     git status

      #     echo "\nCurrent branch:"
      #     git branch --show-current

      #     echo "\nAdding all changes..."
      #     git add -A

      #     echo "\nGit status after add:"
      #     git status

      #     # Only commit if there are changes
      #     if git diff --staged --quiet; then
      #       echo "No changes to commit"
      #       exit 0
      #     else
      #       echo "\nCommitting changes..."
      #       git commit -m "feat: update generated Blue artifacts [skip ci]"

      #       echo "\nVerifying commit:"
      #       git log -1 --stat

      #       echo "\nPushing to origin..."
      #       git push origin $BRANCH_NAME

      #       echo "\nVerifying remote branches:"
      #       git ls-remote --heads origin $BRANCH_NAME
      #     fi

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.WORKFLOW_PAT }}
          title: 'feat: update generated Blue artifacts'
          body: |
            This PR contains automatically generated Blue artifacts.

            Generated by the process-blue-artifacts workflow.
          base: ${{ env.BASE_BRANCH }}
          commit-message: 'feat: update generated Blue artifacts'
          labels: automated-pr, blue-artifacts

      - name: Check outputs
        if: ${{ steps.cpr.outputs.pull-request-number }}
        run: |
          echo "Pull Request Number - ${{ steps.cpr.outputs.pull-request-number }}"
          echo "Pull Request URL - ${{ steps.cpr.outputs.pull-request-url }}"
