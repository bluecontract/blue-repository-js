{
  "name": "@blue-repository/types",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/types/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build:types",
        "cwd": "{workspaceRoot}"
      },
      "outputs": ["{workspaceRoot}/libs/types/dist"]
    }
  }
}
