# @blue-repository/types

This package is generated from `BlueRepository.blue` (source-of-truth: [@bluecontract/blue-repository](https://github.com/bluecontract/blue-repository)).

It contains:

- Repository meta + package registry (`@blue-repository/types/meta`, `@blue-repository/types/repository`)
- Per-package Blue IDs (`@blue-repository/types/packages/*/blue-ids`)
- Per-type contents (`@blue-repository/types/packages/*/contents/*`)
- Per-type Zod schemas (`@blue-repository/types/packages/*/schemas/*`)

## Repository Information

- Repository name: **Blue Repository**
- RepoBlueId: **58apGqo2EExaKTQQJmjASbj8p7tHvrWkoQVjWeWzaox5**

## Installation

```bash
npm install @blue-repository/types
# or
yarn add @blue-repository/types
```

## Notes

- This package is generated; do not edit `libs/types/src` manually.
- In this repo, regenerate with `npm run generate:types` (and build with `npm run build:types`).
